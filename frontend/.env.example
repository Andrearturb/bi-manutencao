# Local development (localhost)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Use only when backend AUTH_MODE=microsoft
NEXT_PUBLIC_AZURE_TENANT_ID=
NEXT_PUBLIC_AZURE_CLIENT_ID=
# Opcional: ex. http://localhost:3000
# NEXT_PUBLIC_AZURE_REDIRECT_URI=

# Note: For Docker, the API_BASE_URL is set in docker-compose.yml
# and will override this value with http://backend:8000
